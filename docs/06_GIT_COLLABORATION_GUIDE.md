# 📘 คู่มือ Git Collaboration — สอนละเอียดทุกขั้นตอน

> สำหรับ: เจ้าของ Backend repo ที่อยากเชิญเพื่อน (Frontend dev) มาทำงานร่วมกัน  
> Platform: **GitHub** (ใช้ GitLab / Bitbucket ก็คล้ายกัน)

---

## 📋 สารบัญ

1. [สร้าง GitHub Account](#1-สร้าง-github-account)
2. [สร้าง Repository](#2-สร้าง-repository-บน-github)
3. [Push โค้ดขึ้น GitHub](#3-push-โค้ด-backend-ขึ้น-github)
4. [เชิญเพื่อนเข้า Repo](#4-เชิญเพื่อนเข้ามาทำงานร่วมกัน)
5. [เพื่อน Clone และเริ่มทำงาน](#5-เพื่อน-clone-repo-และเริ่มทำงาน)
6. [วิธีทำงานร่วมกันแบบ Branch](#6-วิธีทำงานร่วมกันแบบ-branch)
7. [แก้ Conflict](#7-แก้-conflict)
8. [กฏที่ควรตกลงกัน](#8-กฏที่ควรตกลงกันในทีม)

---

## 1. สร้าง GitHub Account

ถ้ายังไม่มี ไปสมัครที่ [github.com](https://github.com) ครับ ทั้งคุณและเพื่อนต้องมีคนละ account

---

## 2. สร้าง Repository บน GitHub

### วิธีสร้าง:

1. เข้า [github.com](https://github.com) แล้ว Login
2. กดปุ่ม **"+"** มุมขวาบน → เลือก **"New repository"**
3. ตั้งค่าดังนี้:

| ตั้งค่า | ค่า |
|---|---|
| Repository name | `auction-api` |
| Description | ระบบประมูลสินค้า - Backend API |
| Visibility | **Private** (แนะนำ) หรือ Public |
| Initialize with README | ❌ **ไม่ต้องติ๊ก** (เพราะเรามีโค้ดอยู่แล้ว) |
| .gitignore | ❌ ไม่ต้องเลือก |
| License | ❌ ไม่ต้องเลือก |

4. กด **"Create repository"**

> ⚠️ **สำคัญ:** ตอนสร้างอย่าติ๊ก Initialize with README เพราะเราจะ push โค้ดที่มีอยู่แล้วขึ้นไป

---

## 3. Push โค้ด Backend ขึ้น GitHub

เปิด Terminal แล้วทำตามนี้:

### 3.1 เข้าไปที่โฟลเดอร์ project

```bash
cd /path/to/auction-api
```

### 3.2 สร้าง .gitignore (สำคัญมาก!)

สร้างไฟล์ `.gitignore` เพื่อไม่ให้ไฟล์ที่ไม่ควร push ขึ้นไปด้วย:

```gitignore
# Laravel
/vendor/
/node_modules/
/.env
/storage/*.key
/storage/logs/*
/bootstrap/cache/*

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build
/public/hot
/public/storage
```

### 3.3 เริ่มต้น Git และ Push

```bash
# 1. สร้าง Git repo ในเครื่อง (ถ้ายังไม่ได้ทำ)
git init

# 2. เพิ่มไฟล์ทั้งหมด
git add .

# 3. Commit ครั้งแรก
git commit -m "Initial commit: Auction API backend"

# 4. ตั้งชื่อ branch หลักเป็น main
git branch -M main

# 5. เชื่อมกับ GitHub repo (เปลี่ยน USERNAME เป็น username ของคุณ)
git remote add origin https://github.com/USERNAME/auction-api.git

# 6. Push ขึ้น GitHub!
git push -u origin main
```

> 💡 **ถ้าถูกถาม username/password:**  
> GitHub ไม่รับ password แล้ว ต้องใช้ **Personal Access Token** แทน  
> ดูวิธีสร้างใน [ขั้นตอนเสริม A](#ขั้นตอนเสริม-a-สร้าง-personal-access-token)

### 3.4 ตรวจสอบ

เข้า GitHub repo ของคุณ ถ้าเห็นไฟล์โค้ดทั้งหมด = สำเร็จ ✅

---

## 4. เชิญเพื่อนเข้ามาทำงานร่วมกัน

### 4.1 เพิ่ม Collaborator

1. เข้า GitHub repo → กดแท็บ **"Settings"**
2. เมนูซ้าย → กด **"Collaborators"** (อยู่ในหมวด Access)
3. กด **"Add people"**
4. พิมพ์ **GitHub username** หรือ **email** ของเพื่อน
5. กด **"Add"**

### 4.2 เพื่อนต้องรับ Invitation

- เพื่อนจะได้รับ **email** เชิญเข้า repo
- หรือเข้าไปที่ [github.com/notifications](https://github.com/notifications) แล้วกด **Accept**
- เมื่อ Accept แล้ว เพื่อนจะสามารถ push/pull โค้ดได้

---

## 5. เพื่อน Clone Repo และเริ่มทำงาน

### ส่งข้อความนี้ให้เพื่อน:

```
สวัสดี! เชิญเข้า repo แล้วนะ ทำตามนี้เลย:

1. Accept invitation ใน email หรือ github.com/notifications
2. เปิด Terminal แล้วรัน:

   git clone https://github.com/USERNAME/auction-api.git
   cd auction-api

3. สร้าง branch ของตัวเองก่อนเริ่มทำงาน:

   git checkout -b frontend/ชื่อ-feature

4. ทำงานเสร็จแล้ว push ขึ้นมา:

   git add .
   git commit -m "เพิ่ม feature อะไร"
   git push origin frontend/ชื่อ-feature

5. แล้วสร้าง Pull Request บน GitHub ให้ผมรีวิวนะ
```

---

## 6. วิธีทำงานร่วมกันแบบ Branch

### 🌿 Branch Strategy แนะนำ:

```
main                    ← branch หลัก (production-ready)
├── develop             ← branch สำหรับรวมงาน
│   ├── feature/login-api       ← คุณทำ Backend
│   ├── feature/login-screen    ← เพื่อนทำ Frontend
│   ├── feature/auction-api     ← คุณทำ Backend
│   └── feature/auction-screen  ← เพื่อนทำ Frontend
```

### 6.1 สร้าง branch `develop`

```bash
# สร้าง develop branch
git checkout -b develop
git push -u origin develop
```

### 6.2 ทุกครั้งที่จะเริ่มทำ feature ใหม่

```bash
# 1. กลับไป develop ก่อน
git checkout develop

# 2. ดึงโค้ดล่าสุด
git pull origin develop

# 3. สร้าง branch ใหม่สำหรับ feature
git checkout -b feature/ชื่อ-feature

# 4. ทำงาน... แก้โค้ด... เสร็จแล้ว commit
git add .
git commit -m "feat: เพิ่ม feature อะไร"

# 5. Push ขึ้น GitHub
git push origin feature/ชื่อ-feature
```

### 6.3 สร้าง Pull Request (PR)

1. เข้า GitHub repo
2. จะเห็นแถบ **"Compare & pull request"** → กดเลย
3. เลือก base branch เป็น `develop`
4. เขียนรายละเอียดว่าทำอะไร
5. กด **"Create pull request"**
6. ให้อีกคน **Review** แล้วกด **"Merge"**

### 6.4 หลัง Merge แล้ว

```bash
# กลับมา develop แล้ว pull โค้ดล่าสุด
git checkout develop
git pull origin develop

# ลบ branch เก่าที่ merge แล้ว (optional)
git branch -d feature/ชื่อ-feature
```

---

## 7. แก้ Conflict

เมื่อทั้งสองคนแก้ไฟล์เดียวกัน Git จะแจ้ง **conflict**

### 7.1 วิธีแก้:

```bash
# 1. ดึงโค้ดล่าสุดจาก develop
git checkout develop
git pull origin develop

# 2. กลับไป branch ของตัวเอง
git checkout feature/my-feature

# 3. Merge develop เข้ามา
git merge develop
```

### 7.2 ถ้ามี Conflict จะเห็นแบบนี้ในไฟล์:

```
<<<<<<< HEAD
// โค้ดของคุณ
$price = $auction->current_price;
=======
// โค้ดของเพื่อน
$price = $auction->starting_price;
>>>>>>> develop
```

### 7.3 วิธีแก้:

1. **เปิดไฟล์ที่ conflict** ใน VS Code (จะ highlight สีให้)
2. **เลือก** ว่าจะเอาของใคร หรือรวมกัน
3. **ลบ** เครื่องหมาย `<<<<<<<`, `=======`, `>>>>>>>`
4. **Save → Commit**

```bash
git add .
git commit -m "fix: resolve merge conflict"
git push origin feature/my-feature
```

---

## 8. กฏที่ควรตกลงกันในทีม

| กฏ | รายละเอียด |
|---|---|
| 🚫 ห้าม push ตรงไป `main` | ใช้ PR เสมอ |
| 📝 เขียน commit message ชัดเจน | เช่น `feat: เพิ่ม login API` / `fix: แก้ bug ราคา` |
| 🔀 สร้าง PR ทุกครั้ง | ให้อีกคน review ก่อน merge |
| 📥 Pull ก่อน Push เสมอ | `git pull` ก่อน `git push` |
| 🏷️ ตั้งชื่อ branch ให้สื่อ | `feature/xxx`, `fix/xxx`, `hotfix/xxx` |
| 📄 อย่า commit `.env` | ใส่ใน `.gitignore` |

---

## ขั้นตอนเสริม A: สร้าง Personal Access Token

GitHub ไม่รับ password ในการ push แล้ว ต้องใช้ Token แทน:

1. เข้า GitHub → กดรูปโปรไฟล์ขวาบน → **Settings**
2. เลื่อนลงล่างสุด → **Developer settings**
3. เลือก **Personal access tokens** → **Tokens (classic)**
4. กด **"Generate new token"** → **"Generate new token (classic)"**
5. ตั้งค่า:
   - **Note:** `auction-api-access`
   - **Expiration:** 90 days (หรือ No expiration)
   - **Scopes:** ติ๊ก ✅ `repo` (ทั้งหมวด)
6. กด **"Generate token"**
7. **Copy token เก็บไว้!** (จะเห็นได้แค่ครั้งเดียว)
8. ตอน `git push` ถาม password → **ใส่ token แทน**

### แนะนำ: ใช้ SSH Key แทน (สะดวกกว่า)

```bash
# 1. สร้าง SSH Key
ssh-keygen -t ed25519 -C "your-email@example.com"

# 2. กด Enter ทั้งหมด (ใช้ค่า default)

# 3. Copy key
pbcopy < ~/.ssh/id_ed25519.pub

# 4. เข้า GitHub → Settings → SSH and GPG keys → New SSH key
#    วาง key ที่ copy มา → กด Add

# 5. เปลี่ยน remote URL เป็น SSH
git remote set-url origin git@github.com:USERNAME/auction-api.git

# 6. ทดสอบ
ssh -T git@github.com
# ถ้าเห็น "Hi USERNAME! You've successfully authenticated" = สำเร็จ ✅
```

---

## 🔄 สรุป Flow การทำงานประจำวัน

```
เช้า:
  git checkout develop
  git pull origin develop
  git checkout -b feature/วัน-นี้-ทำอะไร

ทำงาน:
  (เขียนโค้ด...)
  git add .
  git commit -m "feat: เพิ่ม xxx"

เย็น / เสร็จงาน:
  git push origin feature/วัน-นี้-ทำอะไร
  → สร้าง Pull Request บน GitHub
  → ให้เพื่อน Review + Merge
```

---

> 💡 **Tips:** ถ้าใช้ VS Code ลง Extension **"GitLens"** จะช่วยให้ดู history, blame, branch ได้ง่ายขึ้นมากครับ!
