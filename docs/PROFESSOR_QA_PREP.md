# 🎓 เตรียมตัวคุยกับอาจารย์ — BidKhong Auction API

**วันที่เตรียม:** 20 กุมภาพันธ์ 2026

---

## 1. 🔍 "โปรเจกต์นี้ทำเกี่ยวกับอะไร?"

**ตอบ:** ระบบ API สำหรับเว็บ/แอปประมูลสินค้าออนไลน์ ชื่อ **BidKhong**
- Backend ใช้ **Laravel 12** + **MySQL** (Docker)
- Authentication ใช้ **Laravel Sanctum** (Token-based)
- มี **40 API endpoints** ครอบคลุม 10 ระบบหลัก
- รองรับ **ประมูลแบบ real-time**, ระบบ **Wallet**, **Escrow**, **Post-Auction flow**
- แบ่งงาน: **Backend (ตัวเรา)** / **Frontend (เพื่อน — React Native)**

---

## 2. 📐 "ใช้ Architecture / Design Pattern อะไร?"

**ตอบ:**
- **MVC Architecture** — Model / View(API Response) / Controller
- **RESTful API** — ทุก endpoint ตาม REST conventions (GET, POST, PATCH, DELETE)
- **Token-based Authentication** — Sanctum สร้าง Personal Access Token
- **Database Transaction** — ใช้ `DB::transaction()` ป้องกัน race condition ตอน bid
- **Middleware** — Rate Limiting, Auth Guard, Admin Guard
- **Scheduler** — Auto-close expired auctions ทุก 1 นาที

---

## 3. 🗄 "Database ออกแบบยังไง? มีกี่ตาราง?"

**ตอบ:** มี **13 ตาราง** + 23 migration files

| ตาราง | หน้าที่ | ความสัมพันธ์ |
|-------|--------|-------------|
| `users` | ข้อมูลผู้ใช้ | 1:1 → wallet, 1:M → products, bids, orders |
| `wallets` | กระเป๋าเงิน | 1:M → wallet_transactions |
| `wallet_transactions` | ประวัติธุรกรรม | FK → wallets, users |
| `products` | สินค้าประมูล | FK → categories, subcategories, users |
| `product_images` | รูปสินค้าเพิ่มเติม | FK → products |
| `categories` | หมวดหมู่หลัก (6) | 1:M → subcategories |
| `subcategories` | หมวดหมู่ย่อย (36) | FK → categories |
| `bids` | การประมูล | FK → users, products |
| `orders` | คำสั่งซื้อ | FK → users (buyer + seller), products |
| `order_confirmations` | ข้อมูลติดต่อหลังประมูล | FK → orders, users |
| `disputes` | การแจ้งปัญหาสินค้า | FK → orders, users |
| `user_strikes` | บันทึกการลงโทษ | FK → users |
| `notifications` | แจ้งเตือน | FK → users, products |

**Relationships:**
```
User (1) ── (1) Wallet ── (M) WalletTransactions
User (1) ── (M) Products ── (M) Bids
                          ── (M) ProductImages
                          ── (1) Order ── (M) OrderConfirmations
                                       ── (1) Dispute
Category (1) ── (M) Subcategory
User (1) ── (M) Notifications
User (1) ── (M) UserStrikes
```

---

## 4. 🔨 "ระบบประมูลทำงานยังไง?"

**ตอบ:**

### Flow การประมูล:
1. **Seller** สร้างสินค้า → กำหนดราคาเริ่มต้น, buyout price, bid_increment, ระยะเวลา
2. **Bidder** ต้อง login + มีเงินใน wallet พอ → bid ราคาที่ ≥ current_price + bid_increment
3. เมื่อ bid สำเร็จ:
   - หักเงินจาก wallet → ย้ายไป `balance_pending`
   - คนก่อนหน้าถูก outbid → **refund อัตโนมัติ** + ส่ง notification
   - อัปเดต `current_price` ของสินค้า
4. **Buy Now** — ซื้อทันทีในราคา buyout → จบประมูลเลย
5. **เมื่อหมดเวลา** → Scheduler ปิดอัตโนมัติ → สร้าง Order → แจ้งเตือน

### ป้องกันปัญหา:
- ✅ ห้าม bid สินค้าตัวเอง
- ✅ ต้องมีเงินพอจึง bid ได้
- ✅ ใช้ DB Transaction ป้องกัน race condition (2 คน bid พร้อมกัน)
- ✅ Rate limit ป้องกัน spam

---

## 5. 💰 "ระบบ Wallet / เงินทำงานยังไง?"

**ตอบ:** เป็น **Simulated Wallet** (จำลอง ไม่เชื่อมจริง)
- **balance_available** = เงินที่ใช้ได้
- **balance_pending** = เงินที่ถูกกันไว้ (กำลัง bid อยู่)
- **balance_total** = available + pending

### Flow เงิน:
```
Topup → เพิ่ม balance_available
Bid → หัก available → เพิ่ม pending
Outbid → คืน pending → กลับ available (refund)
ชนะประมูล → pending → โอนให้ seller
Withdraw → หัก available
```

> 💡 ถ้าต้องการเชื่อม payment gateway จริง (เช่น Omise, Stripe) สามารถต่อได้ที่ topup/withdraw endpoints

---

## 6. 🤝 "หลังประมูลจบทำอะไร? (Post-Auction)"

**ตอบ:** มีระบบ **Escrow** (ค้ำประกัน) 5 ขั้นตอน:

```
ประมูลจบ → Buyer/Seller ยืนยัน (48 ชม.)
         → Seller จัดส่ง (3 วัน)
         → Buyer กดรับสินค้า (7 วัน)
         → เงินโอนให้ Seller ✅
         
ถ้ามีปัญหา → Buyer แจ้ง Dispute → Admin ตัดสิน
```

### Timeout Protection:
| ขั้นตอน | เวลา | ถ้าหมดเวลา |
|---------|------|-----------|
| ยืนยัน | 48 ชม. | ยกเลิก + strike |
| จัดส่ง | 3 วัน | ยกเลิก + คืนเงิน + strike |
| กดรับ | 7 วัน | Auto-complete (เงินโอนเลย) |

---

## 7. 🛡 "ป้องกัน Security ยังไง?"

**ตอบ:**

| เรื่อง | วิธีป้องกัน |
|--------|-----------|
| Authentication | Laravel Sanctum Token (ไม่ใช่ session) |
| Password | bcrypt hash (ไม่เก็บ plaintext) |
| Rate Limit | Auth: 10/min, Public: 60/min, Protected: 100/min |
| IDOR (เข้าถึงข้อมูลคนอื่น) | scope ตาม user_id ทุก endpoint |
| Race Condition | DB::transaction() + lock ตอน bid |
| SQL Injection | Eloquent ORM (parameterized query) |
| XSS | API-only (ไม่ render HTML) |
| File Upload | Validate type + size (max 5MB) |

---

## 8. 🐳 "ทำไมใช้ Docker?"

**ตอบ:**
- **Environment consistency** — ทุกคนในทีมได้ PHP, MySQL version เดียวกัน
- **ง่ายต่อการ deploy** — แค่ `docker compose up -d`
- **แยก service** — PHP (app) กับ MySQL (db) แยกกัน
- **ไม่ต้องติดตั้ง PHP/MySQL บนเครื่องจริง**

---

## 9. 📱 "Frontend เชื่อมกับ Backend ยังไง?"

**ตอบ:**
- Frontend ใช้ **React Native** (เพื่อนทำ)
- เชื่อมผ่าน **RESTful API** — ส่ง HTTP request + รับ JSON response
- ใช้ **Bearer Token** ส่งไปกับทุก request ที่ต้อง login
- รูปภาพ serve ผ่าน `http://SERVER_IP:8000/storage/{path}`
- มี **เอกสาร API** (`docs/05_FRONTEND_HANDOFF.md`) ให้ Frontend team

---

## 10. 📊 "ทำไปถึงไหนแล้ว? เหลืออะไรอีก?"

**ตอบ:**

### ✅ Backend — เสร็จ 100%
- 40 endpoints ทดสอบผ่านหมด
- Seed data 27 สินค้า + bidders + bids
- Docker + XAMPP ใช้งานได้
- เอกสารครบ 7 ไฟล์

### 🔄 Frontend — กำลังทำ
- UI ออกแบบเสร็จ
- กำลังเชื่อมต่อ API (เปลี่ยนจาก Mock data → API จริง)

### 📋 สิ่งที่เหลือ:
- Frontend เชื่อม API ให้ครบ
- ทดสอบ end-to-end (bid → ชนะ → จัดส่ง → รับของ)
- อัปเดท ER Diagram ให้ตรงกับ code จริง

---

## 11. 🔧 "เจอปัญหาอะไรบ้างระหว่างทำ?"

**ตอบ:**
1. **Duration type bug** — FormData ส่ง `duration` เป็น string แต่ Carbon ต้องการ int → ใช้ `(int)` cast แก้
2. **Timezone** — Laravel default UTC ทำให้เวลาไม่ตรง → เปลี่ยนเป็น `Asia/Bangkok`
3. **Docker symlink** — Storage symlink ชี้ Docker path → ใช้ XAMPP serve แทน
4. **Race condition ตอน bid** — 2 คน bid พร้อมกัน → แก้ด้วย `DB::transaction()`
5. **ER Diagram ไม่ตรง** — ER เดิมขาด 8 ตาราง → สร้างเอกสาร review แนะนำแก้

---

## 12. 🏗 "แบ่งงานกันในทีมยังไง?"

**ตอบ:**
- **Backend (ตัวเรา):** Laravel API, Database, Docker, Testing, Documentation
- **Frontend (เพื่อน/จอน):** React Native UI, เชื่อม API, Navigation
- เชื่อมกันผ่าน **Git** (push/pull) + ทดสอบผ่าน **Postman**
- มี **Seed data** ให้เพื่อนรัน `migrate:fresh --seed` ได้ data เหมือนกัน

---

## 13. 💡 "ถ้าจะพัฒนาต่อจะทำอะไร? (Future Work)"

**ตอบ:**
1. **Real-time notifications** — ใช้ WebSocket/Pusher แทน polling
2. **Payment Gateway จริง** — เชื่อม Omise/Stripe สำหรับ topup/withdraw
3. **Image CDN** — ใช้ AWS S3 หรือ Cloudflare R2 แทน local storage
4. **Chat ระหว่าง buyer-seller** — เพิ่มระบบแชทหลังประมูลจบ
5. **Rating & Review** — ให้คะแนน buyer/seller หลังรับของ
6. **Push Notifications** — Firebase Cloud Messaging สำหรับแอปมือถือ
7. **Admin Dashboard** — หน้าเว็บจัดการ reports, disputes, users

---

## 14. 🔢 "สรุปตัวเลขสำคัญ"

| รายการ | จำนวน |
|--------|-------|
| API Endpoints | 40 |
| Database Tables | 13 |
| Migration Files | 23 |
| หมวดหมู่สินค้า | 6 หมวด + 36 ย่อย |
| Seed Products | 27 ชิ้น |
| Test Bidders | 10 accounts |
| เอกสาร | 7 ไฟล์ |
| Framework | Laravel 12 |
| Database | MySQL 8 |
| Auth | Sanctum (Token) |
| Container | Docker Compose |
| Frontend | React Native |
